import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    containerDisplay:{
        flex: 1,
        backgroundColor: "#000",
        width: '100%'
    },
    txtDisplay:{
        color: '#fff'
    }
});

export default styles;